<Window x:Class="ShiftCalendar.Views.ManageAbsenceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление статусом сотрудника" Height="650" Width="700"
        Background="#1a1a1a" Foreground="#e8e8e8"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <!-- Стиль для ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#e8e8e8"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="IsSynchronizedWithCurrentItem" Value="True"/>
            <!-- ВАЖНО! -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Background="{TemplateBinding Background}"
                                        Foreground="{TemplateBinding Foreground}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"
                                                                Content="{TemplateBinding Content}"
                                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                Margin="{TemplateBinding Padding}"
                                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                <Path Grid.Column="1"
                                                      x:Name="Arrow"
                                                      Data="M0,0 L4,4 8,0 Z"
                                                      Fill="#e8e8e8"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                                       Background="#2d2d2d"
                                       BorderBrush="#555555"
                                       BorderThickness="1"
                                       CornerRadius="4"
                                       MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                       MinWidth="{Binding ActualWidth, ElementName=ToggleButton}">
                                    <ScrollViewer CanContentScroll="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ToggleButton" Property="Background" Value="#3d3d3d"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для ComboBoxItem -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#e8e8e8"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                               Background="{TemplateBinding Background}"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3d6a9e"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2a5a8a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#e8e8e8"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CaretBrush" Value="#e8e8e8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinHeight" Value="32"/>
        </Style>

        <!-- Стиль для RadioButton -->
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="#e8e8e8"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- Стиль для Button -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3d3d3d"/>
            <Setter Property="Foreground" Value="#e8e8e8"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4d4d4d"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2d2d2d"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для календаря -->
        <Style TargetType="Calendar">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#e8e8e8"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Стиль для дней календаря -->
        <Style TargetType="CalendarDayButton">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#e8e8e8"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarDayButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                Margin="2">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3d6a9e"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2a5a8a"/>
                            </Trigger>
                            <Trigger Property="IsToday" Value="True">
                                <Setter Property="BorderBrush" Value="#4caf50"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsInactive" Value="True">
                                <Setter Property="Foreground" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Выбор сотрудника" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#ffffff"/>

        <!-- Выбор смены -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Смена:" VerticalAlignment="Center" FontSize="14" Foreground="#cccccc"/>
            <ComboBox Grid.Column="1" x:Name="TeamSelector" 
                     SelectionChanged="TeamSelector_SelectionChanged"
                     DisplayMemberPath="Name" SelectedIndex="0"/>
        </Grid>

        <!-- Выбор сотрудника -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Сотрудник:" VerticalAlignment="Center" FontSize="14" Foreground="#cccccc"/>
            <ComboBox Grid.Column="1" x:Name="EmployeeSelector"
                     DisplayMemberPath="FullName" SelectedIndex="0"/>
        </Grid>

        <TextBlock Grid.Row="3" Text="Период отсутствия:" FontSize="14" Margin="0,0,0,10" FontWeight="Bold" Foreground="#ffffff"/>

        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Даты -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="С:" VerticalAlignment="Center" Foreground="#cccccc" FontSize="14"/>

                <Grid Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="StartDateText" Grid.Column="0" Text="Выберите дату" IsReadOnly="True" Background="#2d2d2d" Foreground="#cccccc" MinHeight="32"/>
                    <Button x:Name="StartDateButton" Grid.Column="1" Content="📅" Background="#2d2d2d" BorderBrush="#555555" BorderThickness="1" MinHeight="32" Click="StartDateButton_Click" FontSize="14"/>
                </Grid>

                <TextBlock Grid.Column="2" Text="По:" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="#cccccc" FontSize="14"/>

                <Grid Grid.Column="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="EndDateText" Grid.Column="0" Text="Выберите дату" IsReadOnly="True" Background="#2d2d2d" Foreground="#cccccc" MinHeight="32"/>
                    <Button x:Name="EndDateButton" Grid.Column="1" Content="📅" Background="#2d2d2d" BorderBrush="#555555" BorderThickness="1" MinHeight="32" Click="EndDateButton_Click" FontSize="14"/>
                </Grid>
            </Grid>

            <!-- Тип отсутствия -->
            <TextBlock Grid.Row="1" Text="Тип отсутствия:" FontSize="14" Margin="0,0,0,10" FontWeight="Bold" Foreground="#ffffff"/>

            <StackPanel Grid.Row="2" Margin="0,0,0,20">
                <RadioButton x:Name="VacationRadio" Content="🏖️ Отпуск" GroupName="AbsenceType" Tag="Отпуск"/>
                <RadioButton x:Name="SickRadio" Content="🏥 Больничный" GroupName="AbsenceType" Tag="Больничный"/>
                <RadioButton x:Name="DayOffRadio" Content="😴 Отгул" GroupName="AbsenceType" Tag="Отгул"/>
                <RadioButton x:Name="OnShiftRadio" Content="✅ На смене (отменить отсутствие)" GroupName="AbsenceType" Tag="НаСмене"/>
            </StackPanel>

            <!-- Комментарий -->
            <TextBlock Grid.Row="3" Text="Комментарий:" FontSize="14" Margin="0,0,0,8" FontWeight="Bold" Foreground="#ffffff"/>
            <TextBox Grid.Row="4" x:Name="CommentTextBox" Height="80" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="60"/>
        </Grid>

        <!-- Кнопки -->
        <Grid Grid.Row="5" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Отмена" Click="CancelButton_Click" Margin="0,0,5,0" Background="#3d3d3d"/>
            <Button Grid.Column="1" Content="Сохранить" Click="SaveButton_Click" Background="#2e7d32" Foreground="White" BorderBrush="#4caf50"/>
        </Grid>
    </Grid>
</Window>